# Data sources / exchange clients
ccxt>=4.3.0
alpaca-py>=0.20.0

# Data manipulation
pandas>=2.2.0
numpy>=1.26.0

# Parquet I/O
pyarrow>=15.0.0
fastparquet>=2024.2.0       # fallback engine

# Feature engineering
nolds>=0.5.2                # Hurst / DFA

# Environment & config
python-dotenv>=1.0.0

# Async HTTP
aiohttp>=3.9.0
# asyncio is part of the standard library — no pip install required

# Logging
loguru>=0.7.2

# Backtester
vectorbt>=0.26.0
scipy>=1.12.0
tabulate>=0.9.0
