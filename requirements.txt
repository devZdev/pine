# Data sources / exchange clients
ccxt>=4.3.0
alpaca-py>=0.20.0

# Data manipulation
pandas>=2.2.0
numpy>=1.26.0

# Parquet I/O
pyarrow>=15.0.0
fastparquet>=2024.2.0       # fallback engine

# Feature engineering
nolds>=0.5.2                # Hurst / DFA

# Environment & config
python-dotenv>=1.0.0

# Async HTTP
aiohttp>=3.9.0
# asyncio is part of the standard library — no pip install required

# Logging
loguru>=0.7.2

# Backtester
vectorbt>=0.26.0
scipy>=1.12.0
tabulate>=0.9.0

# Phase 2 — Black Box regime API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
transformers>=4.40.0
# torch is installed separately in the Dockerfile with the CPU-only index URL
# to avoid pulling the large CUDA build.  For local dev outside Docker you can
# install it manually:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
accelerate>=0.30.0
# chronos-forecasting is installed separately in the Dockerfile:
#   pip install chronos-forecasting

# Test suite (offline)
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0       # FastAPI TestClient
freezegun>=1.4.0    # time-dependent tests
